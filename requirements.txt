# PreProConvert - Dependencies
fastapi>=0.100.0
uvicorn[standard]>=0.22.0
python-multipart>=0.0.6
aiofiles>=23.0.0

# Core conversion dependencies
# Note: PyAlembic and imath are provided by the ASWF Docker image
numpy>=1.24.0
